# DFIR profile: windows_browser_cache
#
# Curated bundle of reviewed, catalog-verified Velociraptor artifacts.
# Every artifact below is present in catalog/artifacts.yaml and must also
# be added to policy.allowed_artifacts before this profile can be used.
# Definition only: adding this file adds no collection/hunt/VQL logic.
name: windows_browser_cache
display_name: "Windows Browser Cache"
description: >
  Edge favicon cache and Chrome session storage (visited-site / session evidence). Privacy-sensitive; approval-gated.
target_os: windows
category: browser
risk_level: high
requires_approval: true
max_runtime_seconds: 600
max_result_rows: 20000
max_result_bytes: 20971520
artifacts:
  - name: Windows.Applications.Edge.Favicons
  - name: Generic.Applications.Chrome.SessionStorage
