# DFIR profile: windows_powershell_activity
#
# Curated bundle of reviewed, catalog-verified Velociraptor artifacts.
# Every artifact below is present in catalog/artifacts.yaml and must also
# be added to policy.allowed_artifacts before this profile can be used.
# Definition only: adding this file adds no collection/hunt/VQL logic.
name: windows_powershell_activity
display_name: "Windows PowerShell Activity"
description: >
  PowerShell scriptblock and module logging plus per-user console history (PSReadline). Console history may contain typed secrets, so this profile is approval-gated.
target_os: windows
category: powershell
risk_level: high
requires_approval: true
max_runtime_seconds: 600
max_result_rows: 5000
max_result_bytes: 10485760
artifacts:
  - name: Windows.EventLogs.PowershellScriptblock
  - name: Windows.EventLogs.PowershellModule
  - name: Windows.System.Powershell.PSReadline
