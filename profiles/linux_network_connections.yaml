# DFIR profile: linux_network_connections
#
# Curated bundle of reviewed, catalog-verified Velociraptor artifacts.
# Every artifact below is present in catalog/artifacts.yaml and must also
# be added to policy.allowed_artifacts before this profile can be used.
# Definition only: adding this file adds no collection/hunt/VQL logic.
name: linux_network_connections
display_name: "Linux Network Connections"
description: >
  Active network connections, ARP table, and interface addresses.
target_os: linux
category: network
risk_level: medium
requires_approval: false
max_runtime_seconds: 600
max_result_rows: 5000
max_result_bytes: 10485760
artifacts:
  - name: Linux.Network.Netstat
  - name: Linux.Proc.Arp
  - name: Generic.Network.InterfaceAddresses
