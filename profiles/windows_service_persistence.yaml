# DFIR profile: windows_service_persistence
#
# Curated bundle of reviewed, catalog-verified Velociraptor artifacts.
# Every artifact below is present in catalog/artifacts.yaml and must also
# be added to policy.allowed_artifacts before this profile can be used.
# Definition only: adding this file adds no collection/hunt/VQL logic.
name: windows_service_persistence
display_name: "Windows Service Persistence"
description: >
  Installed and critical Windows services plus service-creation event records (7045).
target_os: windows
category: persistence
risk_level: medium
requires_approval: false
max_runtime_seconds: 600
max_result_rows: 5000
max_result_bytes: 10485760
artifacts:
  - name: Windows.System.Services
  - name: Windows.System.CriticalServices
  - name: Windows.EventLogs.ServiceCreationComspec
