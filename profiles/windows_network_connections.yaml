# DFIR profile: windows_network_connections
#
# Curated bundle of reviewed, catalog-verified Velociraptor artifacts.
# Every artifact below is present in catalog/artifacts.yaml and must also
# be added to policy.allowed_artifacts before this profile can be used.
# Definition only: adding this file adds no collection/hunt/VQL logic.
name: windows_network_connections
display_name: "Windows Network Connections"
description: >
  Active connections, listening ports, ARP cache, and DNS resolver cache.
target_os: windows
category: network
risk_level: medium
requires_approval: false
max_runtime_seconds: 600
max_result_rows: 5000
max_result_bytes: 10485760
artifacts:
  - name: Windows.Network.Netstat
  - name: Windows.Network.ListeningPorts
  - name: Windows.Network.ArpCache
  - name: Windows.System.DNSCache
