# DFIR profile: windows_authentication_events
#
# Curated bundle of reviewed, catalog-verified Velociraptor artifacts.
# Every artifact below is present in catalog/artifacts.yaml and must also
# be added to policy.allowed_artifacts before this profile can be used.
# Definition only: adding this file adds no collection/hunt/VQL logic.
name: windows_authentication_events
display_name: "Windows Authentication Events"
description: >
  RDP, alternate-credential, and explicit-logon (4648) authentication event records.
target_os: windows
category: authentication
risk_level: medium
requires_approval: false
max_runtime_seconds: 600
max_result_rows: 5000
max_result_bytes: 10485760
artifacts:
  - name: Windows.EventLogs.RDPAuth
  - name: Windows.EventLogs.AlternateLogon
  - name: Windows.EventLogs.ExplicitLogon
